import { HoverLightDirective } from './hover-light.directive';

describe('HoverLightDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverLightDirective();
    expect(directive).toBeTruthy();
  });
});
